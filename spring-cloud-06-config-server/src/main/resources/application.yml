spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/baihezhuo/learngit  ##git地址
          
server:
  context-path: /
  port: 4000